Async Alembic environment. The database URL is injected from app.core.config
(see env.py); alembic.ini's sqlalchemy.url is intentionally blank.

  uv run alembic revision -m "message"   # new migration
  uv run alembic upgrade head            # apply
  uv run alembic downgrade -1            # roll back one
